# fastgit-sys

Rust FFI bindings to [fastgit](https://github.com/pq-cybarg/fastgit) — bare-metal C git (0.1.1).

```toml
[dependencies]
fastgit-sys = "0.1"
```

Requires `cmake`, `zlib`, `openssl` at build time. `cargo publish` from `packaging/cargo`.
