# fastgit (npm)

Node bindings to fastgit 0.1.1. Requires cmake build first: `npm install` runs `node-gyp rebuild` linking `build/src/libfastgit.*`.

Publish: `npm publish` from `packaging/npm` (needs `npm login`).
